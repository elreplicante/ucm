/*
 * main.cpp
 *
 *  Created on: 14/12/2012
 *      Author: repli
 */

#include "test.h"
#include "ordenacion.h"



const int TINY = 1000;
const int SMALL = 10000;
const int MEDIUM = 50000;
const int BIG = 100000;
const int HUGE = 5000000;

int main() {

	ofstream file;
	file.open("supu.txt");
/*
	int n;
	cout << "How many elements will have your array?:";
	cin >> n;
*/
	int V[TINY], first[SMALL], second[MEDIUM], third[BIG], fourth[HUGE];
	generateArray(V, TINY);
	generateArray(first, SMALL);
	generateArray(second, MEDIUM);
	generateArray(third, BIG);

	generateInsertionSortTime(file, V, TINY);
	generateInsertionSortTime(file, V, SMALL);
	generateInsertionSortTime(file, V, MEDIUM);
	generateInsertionSortTime(file, V, BIG);

	//makeFourCopies(V, first, second, third, fourth, n);



	// INSERTION SORT




/*
	printArrayContents(first, n);

	if (checkIsOrdered(first, n)) {
		cout << "Sorting performed correctly in " << time << " seconds" << endl;
	}
*/
	file.close();

/*

	cout << "Selection sort:" << endl;
	selectionSort(second, n);
	if (checkIsOrdered(second, n)) {
		cout << "Sorting performed correctly.\n";
	}
	printArrayContents(second, n);
	cout << "Bubble sort:" << endl;
	bubbleSort(third, n);
	if (checkIsOrdered(third, n)) {
		cout << "Sorting performed correctly.\n";
	}
	printArrayContents(third, n);
*/

	return 0;
}
