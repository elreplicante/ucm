SELECT
  EMP.NOMBRE,
  EMP.DNI,
  T.TELÉFONO
FROM
  EMPLEADOS EMP,
  TELÉFONOS T
WHERE EMP.DNI IN
  (
  SELECT DISTINCT
    T1.DNI
  FROM
    TELÉFONOS T1,
    TELÉFONOS T2
  WHERE
    T1.DNI = T2.DNI AND
    T1.TELÉFONO <> T2.TELÉFONO
)AND
  EMP.DNI = T.DNI;